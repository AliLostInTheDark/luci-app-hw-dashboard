#!/bin/sh
# Interface transitions change device, address, route and mwan3 mark together;
# flag the collector to rediscover now rather than polling for them.

case "$ACTION" in
	ifup|ifdown|ifupdate|iflink)
		[ -d /tmp/hwdash_wanmon ] && touch /tmp/hwdash_wanmon/.rediscover
		;;
esac
